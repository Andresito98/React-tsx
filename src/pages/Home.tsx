import React from "react";

const Home = () => {
    


    return (
        <div>
            Home
        </div>
    );
};

export default Home;

// const Home = (props: {name: string}) => {
// {props.name ? 'Hola' + props.name : 'No estas logeado en'}